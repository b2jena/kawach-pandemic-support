spring:
  application:
    name: USER-MANAGEMENT-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8076/eureka/
  instance:
    hostname: localhost